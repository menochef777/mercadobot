{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd simple-pdv && npm install && npx prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "cd simple-pdv && npx prisma migrate deploy && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
