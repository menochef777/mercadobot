{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "sh -c 'if [ -d simple-pdv ]; then cd simple-pdv; fi; npm install && npm run build'"
  },
  "deploy": {
    "startCommand": "sh -c 'if [ -d simple-pdv ]; then cd simple-pdv; fi; npx prisma db push && npm start'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
