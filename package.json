{
  "name": "mercadobot",
  "version": "1.0.0",
  "private": true,
  "description": "MercadoBot / GestorMercado Monorepo",
  "scripts": {
    "postinstall": "cd simple-pdv && npm install",
    "build": "cd simple-pdv && npx prisma generate && npm run build",
    "start": "cd simple-pdv && npm start"
  },
  "dependencies": {
    "@prisma/client": "^5.15.0"
  },
  "devDependencies": {
    "prisma": "^5.15.0",
    "typescript": "^5.4.5"
  }
}
